# Repo-wide scripts

Miscellaneous CI helpers and developer scripts that don't belong to a specific package (e.g. local environment bootstrap, release helpers).

**Status:** Phase 0 stub — empty. `tooling/lint/boundaries.sh` (the architecture-boundary checks) already exists as a sibling directory; scripts here are added as later phases need them.
