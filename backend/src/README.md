# `backend/src`

Shared Deno/TypeScript domain code used by Edge Functions in `../supabase/functions/` (domain services, repositories) — the part of the BFF that could, per [ADR-0002](../../docs/adr/0002-backend-bff-edge-functions.md), be re-hosted on a different runtime later without a client-visible change.

**Status:** Phase 0 stub — empty. Populated starting Phase 1.
