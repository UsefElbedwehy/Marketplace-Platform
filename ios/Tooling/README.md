# iOS Tooling

Fastlane lanes and the white-label build pipeline (`whitelabel build --client <c>`) that generate `Info.plist`/entitlements/asset catalogs from `configs/clients/<c>/app.json` and drive signing + TestFlight/App Store delivery. See [docs/planning/07-configuration-whitelabel-theme.md §3](../../docs/planning/07-configuration-whitelabel-theme.md#3-white-label-pipeline-build-time).

**Status:** Phase 0 stub — empty. Implemented in Phase 5 (white-label proof).
